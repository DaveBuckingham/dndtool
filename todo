keep a structure for commands such as 'database' and 'help' and 'quit'
more generators
wrap at actual screen width
travel pace

config file
    set prompt
    shell (for !-passthrough, defaul is bash)
    colors
    terminal width

parameters
    database url



